==> Authentication
    => register (1. user data need to save in db  2. token need to give to user)
    => login
    => logout (token blacklisting)
    => [OTP based registration]

==> post
    => create
    => can see the feed
    => like posts (collection types)
    => save post

==> users
    => followers
    => following


POST API: (multer + READ Doc && ImageKit + READ Doc)
==> post (is not "rew" it is in "from-data")
    => create
    {
        caption: String,
        image_url: String,
        userId: user who created it,
        createdAt
    }
&& imageKit image uplode 
